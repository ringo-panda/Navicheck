<div class="row my-3">
  <div class="col-sm-6">
    <h2>志望度一覧・追加</h2>
  </div>
</div>

<div class="row mb-3 offset-sm-1">
  <%= form_with model:@wish_degree do |f| %>
    <div class="form-row align-items-center">
      <div class="col-sm-auto">
        <%= f.text_field :wish_degree_name, placeholder: "志望度", class:"form-control" %>
      </div>
      <div class="col-sm-auto">
        <%= f.submit "追加する", class:"btn btn-primary" %>
      </div>
    </div>
  <% end %>
</div>

<div class="row">
  <div class="col-sm-10">
    <p class="text-danger">志望度カテゴリは使用されている場合削除できません。また、志望度カテゴリは１つ以上存在している必要があります。</p>
    <table class="table">
      <thead class="thead-light">
        <tr>
          <th style="width: 10%"></th>
          <th style="width: 60%">志望度</th>
          <th style="width: 15%"></th>
          <th style="width: 15%"></th>
        </tr>
      </thead>
      <tbody>
        <% @wish_degrees.each do |post| %>
          <tr>
            <td><%= post.companies.count %></td>
            <td><%= post.wish_degree_name %></td>
            <td><%= link_to "編集する", edit_wish_degree_path(post.id), class:"btn btn-success" %></td>
            <% if post.companies.count == 0 && @wish_degrees.count >= 2 %>
            <td><%= link_to "削除する", wish_degree_path(post.id), method: :delete, data: {confirm: "本当に削除しますか？"}, class:"btn btn-danger" %></td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
